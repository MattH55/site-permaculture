{
  "name": "resilience-quiz-edmonton",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "vercel-build": "echo 'static + serverless — no build step'"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}
